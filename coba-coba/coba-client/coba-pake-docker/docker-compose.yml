version: '3'

services:
  local-inferencer:
    image: muhandre/fogverse:try-client-local
    environment:
    - CONSUMER_SERVERS=localhost:9093
    - CONSUMER_TOPIC=result
    restart: always
    network_mode: host
    # deploy:
    #   resources:
    #     limits:
    #       cpus: '0.75'
    #       memory: 2800M
    #     reservations:
    #       devices:
    #         - capabilities: [gpu]